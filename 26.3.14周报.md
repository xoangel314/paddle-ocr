**本周进展**：成功训练出了一个手写数字识别模型

用PaddlePaddle 框架进行模型训练.运行设备:CPU

解决上周出现问题，第二次修改完计算代码得出结果是：
```Using device: Place(cpu)
Epoch 1, Loss: 0.1439
Epoch 2, Loss: 0.0479
Epoch 3, Loss: 0.0324
Test Accuracy: 7.0735
```
找出原因：测试准确率函数问题——标签维度不一致导致计算异常

解决办法：把labels压缩一维，加上`labels = labels.squeeze()`

最后输出成功结果：
```
Using device: Place(cpu)
Epoch 1, Loss: 0.1515
Epoch 2, Loss: 0.0499
Epoch 3, Loss: 0.0333
Test Accuracy: 0.9876
```
![损失函数](./images/training_loss.png)

**总结**：原系统主要依赖PaddleOCR进行字符识别，但对于手写分数的识别准确率有限。
为提升系统对手写数字的识别能力，引入深度学习训练，先在 MNIST 手写数字数据集上训练卷积神经网络模型，验证模型结构与训练流程的可行性。

**下一步**：在此基础上，后续结合试卷实际数据对模型进行进一步优化
